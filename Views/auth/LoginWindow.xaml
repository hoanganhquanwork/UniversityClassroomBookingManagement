<Window x:Class="UniversityClassroomBookingManagement.Views.auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityClassroomBookingManagement.Views.auth"
        mc:Ignorable="d"
        Title="FPT University - Room Booking System"
        Height="550" Width="958"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">

    <!-- Background image -->
    <Window.Background>
        <ImageBrush ImageSource="/Assets/images/background.png" Stretch="UniformToFill"/>
    </Window.Background>

    <Window.Resources>
        <Style TargetType="Control" x:Key="InputFieldStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="BorderBrush" Value="#B0B0B0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource InputFieldStyle}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource InputFieldStyle}"/>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#0066CC"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Width="362"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Background="White"
                CornerRadius="12"
                Padding="30"
                Margin="28,0,0,0"
                Height="465">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" Direction="270" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>

            <StackPanel>
                <Image Source="/Assets/images/logo.png"
                       Height="55"
                       Margin="0,0,0,15"/>

                <TextBlock Text="Login to the System"
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,25"
                           Foreground="#333"/>

                <TextBlock Text="Username" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="txtUsername"
                         Style="{StaticResource InputFieldStyle}"/>

                <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
                <PasswordBox x:Name="txtPassword"
                             Style="{StaticResource InputFieldStyle}"/>

                <TextBlock HorizontalAlignment="Right" Margin="0,-10,0,15"/>

                <Button x:Name="btnLogin"
                        Content="Login"
                        FontSize="16"
                        FontWeight="Bold"
                        Background="#FF6600"
                        Foreground="White"
                        Padding="12"
                        Cursor="Hand"
                        Click="btnLogin_Click">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
